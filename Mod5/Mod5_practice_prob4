# Outer loop for rows (1 to 5)
for i in range(1, 6):
    # Inner loop for columns (1 to 5)
    for j in range(1, 6):
        # Print the product, formatted to align in columns
        print(f"{i * j:4}", end="")
    # Move to the next line after each row
    print()

