'''#3. Create a list of strings. Write code to create a new 
list that includes only the strings 
longer than three characters. Print the resulting filtered list.'''

words = ["cat", "tree", "hi", "water", "sun", "sky"]
filtered = []

for word in words:
    if len(word) > 3:
        filtered.append(word)

print("Strings longer than 3 characters:", filtered)
